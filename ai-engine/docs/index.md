# AI Engine Documentation

- `architecture.md`: intelligence layers, ownership, and evolution path.
- `api-contract.md`: HTTP boundary consumed by the main application.
- `dataset.md`: current deterministic catalog and provider policy.
- `vector-store.md`: optional retrieval infrastructure boundary.

The root `README.md` remains intentionally short so a contributor can run the
service without opening the full design documents.
