# WayFinder Backend

Public-safe API shell for the WayFinder showcase.

Routes:
- `GET /health`
- `POST /api/demo/plan`
- `POST /api/demo/interactions`

The production platform uses authenticated persistence, private data models, and a deeper planning pipeline. Those internals are intentionally excluded from this public repository.

